namespace dungeon {

    /*!
     * 階段の状態(上向きか下向きか)を表します。
     */
    export enum StairStatus {
        /*!
         * 下向き階段です。
         *
         * (<)
         */
        Down,
        /*!
         * 上向き階段です。
         *
         * (>)
         */
        Up
    }
}
